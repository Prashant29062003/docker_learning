> A container is a runtime instance of a Docker image.
> It is a lightweight, Standalone and executable package that includes everything needed to run a piece of software

1. Isolation: **
2. Ephemeral: **
3. portable: **

*Note*: *best path to check docker commands use `docker container` in terminal.*